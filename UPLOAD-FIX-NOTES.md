# Upload Fix

This build is prepared for Vercel deployment with authenticated upload requests
sending browser credentials to the upload API.

Bucket configuration remains:
S3_BUCKET=tong-an-photography

Do not place Application Key secrets in client-side code.
