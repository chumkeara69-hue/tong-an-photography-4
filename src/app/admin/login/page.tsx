"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
export default function AdminLogin(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setError("");const r=await fetch("/api/auth/login",{method:"POST",credentials:"same-origin",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});if(r.ok) router.push("/admin"); else setError((await r.json()).error||"Login failed");}return <main className="container" style={{padding:"70px 0",maxWidth:520}}><div className="card" style={{padding:28}}><h1>Admin Login</h1><form onSubmit={submit} style={{display:"grid",gap:16,marginTop:20}}><div><label className="label">Email</label><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div><label className="label">Password</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div>{error&&<p style={{color:"#fca5a5"}}>{error}</p>}<button className="btn btn-gold">Login</button></form></div></main>}
